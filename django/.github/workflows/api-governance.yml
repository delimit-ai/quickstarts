name: API Governance
on: [pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  delimit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: delimit-ai/delimit-action@v1
